# 更新日志

本项目所有版本的变更都记录在此文件中。版本号遵循语义化版本规范。

## v0.4.1

### 新增

- 文件菜单新增最近打开入口，分别展示最近打开的 Markdown 文件和文件夹，便于快速恢复常用工作区。

### 优化

- 打开文件、另存为、在新窗口打开文件、打开文件夹时会自动更新最近记录，并限制保留最近 5 项。

## v0.4.0

### 新增

- 主窗口支持多标签页编辑，可在同一个 MDBridge 窗口内打开多个 Markdown 文件并快速切换。
- 文件菜单和标签栏新增“新建标签页”，打开已存在的文件时会激活对应标签页，避免重复打开同一路径。

### 优化

- 标签切换、打开新文件、关闭当前标签页前会先落盘当前文件的待保存编辑，降低切换文档时丢失内容的风险。
- 保留 v0.3.0 的“在新窗口打开文件”能力，与主窗口多标签页能力并存。

## v0.3.0

### 新增

- 支持将 Markdown 文件打开到独立文档窗口，可同时查看和编辑多个文件。
- 文件菜单新增“在新窗口打开文件”，文件树右键菜单新增“在新窗口打开”。

### 优化

- 主窗口继续作为托盘入口关闭后隐藏，独立文档窗口关闭时正常销毁，避免影响其他窗口。

## v0.2.1

### 修复

- 修复微信公众号代码块粘贴后缩进错乱、长行折行续行回到行首的问题。
- 微信公众号代码块改为平台专用的逐行 `section` 渲染，将原始前导空格 / Tab 转换为行级 `padding-left`，避免与 B站、知乎、掘金、推特等其他平台的转换逻辑相互干扰。

### 优化

- 微信公众号代码块新增语法高亮与 macOS 风格标题栏，并补充说明该实现仅用于微信公众号编辑器兼容。

## v0.2.0

### 新增

- 文件树侧栏支持整体左右折叠/展开，折叠后收为窄竖条，腾出更多编辑与预览空间。

### 修复

- 修复微信公众号转换中图片重复渲染的问题（此前每张图片会生成两个 `<img>`）。
- 修复微信公众号列表项以加粗词开头时被强制折行的排版问题，改用带项目符号的段落以适配公众号编辑器。
- 修复中文 / 全角标点旁的加粗、斜体语法失效问题（CommonMark flanking 规则缺陷），后端转换与前端实时预览均已修复。

### 优化

- 微信公众号转换补全列表、引用、分隔线的渲染，并为标题、段落、代码等元素添加行内样式，确保粘贴到公众号编辑器后排版正确。

## v0.1.9

### 优化

1. 版本更新弹窗的更新日志改用有序列表渲染，自动过滤 GitHub Release 中的「Assets 下载说明」，只展示真正的变更条目。
2. 重新设计更新弹窗的更新内容区域样式，增加标题、背景卡片与列表间距，阅读体验更清晰。
3. 更新日志解析器同时识别 `-`、`*` 与 `1.` 等有序列表标记，并补充对应回归测试。

## v0.1.8

### 优化

- 版本更新弹窗中的更新日志改用 Markdown 渲染，列表、加粗等格式可正常展示，不再以纯文本形式显示。

## v0.1.7

### 修复

- 修复实时文件刷新的竞态问题：在异步读取文件期间，如果发生按键输入或切换文件，旧的外部内容可能覆盖本地尚未保存的编辑。现在每次异步读取完成后都会重新检查「本地编辑状态」与「当前激活文件」，确保读取窗口期内的操作不会被旧内容覆盖。
- 新增针对该竞态条件的回归测试，防止后续改动再次引入问题。

## v0.1.6

### 新增

- 支持实时刷新外部修改的 Markdown 文件：当前打开的文件在磁盘上被外部程序修改后，编辑器会自动加载最新内容。

### 文档

- 更新 README 中各平台图片支持状态，修正「B站专栏」为不支持外链图片。

## v0.1.5

### 新增

- 新增版本更新检查功能，可在启动时检测新版本。
- 配置签名版自动更新流程，并上传 updater 元数据用于自动更新。

### 修复

- 修复从 Dock 图标恢复窗口的行为。
