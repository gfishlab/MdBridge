# Git Workflow

## 提交规则

每个 task 完成后必须：
1. `git commit` — 提交代码
2. `git push` — 推送到远程仓库
3. 更新 `.claude/memory/project-progress.md` — 记录进度

## 提交信息格式

```
<type>: <description>

类型：feat, fix, docs, refactor, test, chore
